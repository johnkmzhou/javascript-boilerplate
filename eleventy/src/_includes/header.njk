<div class="flex-1 flex flex-col h-full justify-evenly sm:justify-end">
  <div class="text-center mx-4 sm:mx-8 my-8 flex-grow flex flex-col justify-center">
    <div>
      <h1 class="text-4xl sm:text-5xl font-extrabold tracking-tight">{{heroHeadline}}</h1>
      <div class="text-2xl text-gray-500 my-4 sm:my-6">{{heroSubheading}}</div>
      <a class="btn" href="/">{{heroCallToActionText}}</a>
    </div>
  </div>
  <img class="w-full h-auto" src="{{heroImg.src}}" alt="{{heroImg.alt}}"></img>
</div>
